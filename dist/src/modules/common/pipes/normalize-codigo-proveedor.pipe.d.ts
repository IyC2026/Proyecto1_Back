import { ArgumentMetadata, PipeTransform } from '@nestjs/common';
export declare class NormalizeCodigoProveedorPipe implements PipeTransform {
    transform(value: any, metadata: ArgumentMetadata): any;
}
